class DashboardMetric {
  constructor({ id, title, field, tone, icon, format }) {
    this.id = id
    this.title = title
    this.field = field
    this.tone = tone
    this.icon = icon
    this.format = format
  }
}

export default DashboardMetric
